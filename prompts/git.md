# Prompt — Git Initialization

```txt
Initialize a git repository in the current directory and commit all relevant project files with the message "Initial commit".

Do not commit useless local files such as `.DS_Store`.
Do not add yourself as a co-author. I should be the sole author.
```
